// Этот файл генерируется автоматически при сборке фронтенда
// или может быть заменен при деплое в кластер
window.ENV = {
  // URL для API (по умолчанию относительный путь)
  API_URL: "/api",
  
  // В production этот файл НЕ используется для определения WebSocket URL,
  // так как это значение вычисляется динамически в api.js.
  // Этот параметр будет использован только если его задать явно при деплое.
  WEBSOCKET_URL: null
};